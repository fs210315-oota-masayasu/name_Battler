<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.namebattler">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <!-- テスト用アクティビティ -->

        <activity android:name=".test.NewWordActivity">

        </activity>

        <activity android:name=".test.TestNewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

         <!-- テスト用アクティビティ -->



        <activity android:name=".memu.MainActivity">

            <!--
           テストのためにコメントアウト
           <intent-filter>
               <action android:name="android.intent.action.MAIN" />
               <category android:name="android.intent.category.LAUNCHER" />
           </intent-filter>
            -->
        </activity>
        <activity android:name=".characters.CharacterListActivity">

        </activity>
        <activity android:name=".party.PartyCreateActivity">

        </activity>
        <activity android:name=".characters.NewCharacterCreateActivity">

        </activity>
        <activity android:name=".characters.GeneratedCharacterCompletionActivity">

        </activity>
    </application>

</manifest>